db.sales_work.find(
    { orderId: "LAB_<ROLL>" },       // filter by your orderId
    { 
        _id: 0,                       // exclude _id
        orderId: 1,
        storeLocation: 1,
        purchaseMethod: 1,
        item0: { $arrayElemAt: ["$items.name", 0] }  // first item's name
    }
);
